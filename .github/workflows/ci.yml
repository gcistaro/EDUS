name: EDUS CI (dependency discovery)

on:
  push:
  pull_request:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Get code
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # ab-initio models of the tests (gcistaro/EDUS-models)
          submodules: true

      # 2. Show environment (VERY IMPORTANT for debugging)
      - name: Inspect system
        run: |
          echo "=== COMPILER ==="
          g++ --version || true
          echo "=== CMAKE ==="
          cmake --version || true
          echo "=== PYTHON ==="
          python3 --version || true

      # 3. Install all needed packages
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
                      build-essential \
                      cmake \
                      libopenmpi-dev \
                      openmpi-bin \
                      libhdf5-dev \
                      libfftw3-dev \
                      libfftw3-mpi-dev \
                      libopenblas-dev \
                      liblapack-dev \
                      liblapacke-dev            

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install numpy==1.26.4
          python -m pip install scipy
          python -m pip install matplotlib
          python -m pip install plotly
          python -m pip install h5py
      
      # 4. Clone Spack (but DO NOT install heavy deps yet)
      - name: Clone Spack
        run: git clone --depth=1 https://github.com/spack/spack.git $HOME/spack

      # 5. Register your local Spack repo (safe, no installs yet)
      - name: Register Spack repo
        run: |
          . $HOME/spack/share/spack/setup-env.sh
          spack repo add $GITHUB_WORKSPACE/spack || true
          spack repo list

      # Configure project 
      - name: Configure CMake
        run: |
          cmake -S . -B build -DEDUS_PROFILE=ON -DEDUS_MKL=OFF -DEDUS_MPI=ON 

      # Build (will likely fail if dependencies missing)
      - name: Build project
        run: |
          cmake --build build -j$(nproc) 

      - name: Run unit tests of the time steppers
        run: |
          cd build
          ctest -R "^(TimeStepperTest|MagnusTest)" --output-on-failure

      - name: Run hBN_IPA test
        env:
          MPIEXEC_FLAGS: --oversubscribe
        run: |
          cd build
          ctest -R "^hBN_IPA$" --output-on-failure

      - name: Run hBN_HSEX test
        env:
          MPIEXEC_FLAGS: --oversubscribe
        run: |
          cd build
          ctest -R "^hBN_HSEX$" --output-on-failure

      - name: Run Si_DFT_IPA test
        env:
          MPIEXEC_FLAGS: --oversubscribe
        run: |
          cd build
          ctest -R "^Si_DFT_IPA$" --output-on-failure

      - name: Run Si_IPA test
        env:
          MPIEXEC_FLAGS: --oversubscribe
        run: |
          cd build
          ctest -R "^Si_IPA$" --output-on-failure

      - name: Run Si_IPA_decay test
        env:
          MPIEXEC_FLAGS: --oversubscribe
        run: |
          cd build
          ctest -R "^Si_IPA_decay$" --output-on-failure

      - name: Run Si_IPA_initialtime test
        env:
          MPIEXEC_FLAGS: --oversubscribe
        run: |
          cd build
          ctest -R "^Si_IPA_initialtime$" --output-on-failure

      - name: Run Si_HSEX test
        env:
          MPIEXEC_FLAGS: --oversubscribe
        run: |
          cd build
          ctest -R "^Si_HSEX$" --output-on-failure

      - name: Run Si_IPA_Magnus test
        env:
          MPIEXEC_FLAGS: --oversubscribe
        run: |
          cd build
          ctest -R "^Si_IPA_Magnus$" --output-on-failure

      - name: Run Si_HSEX_Magnus test
        env:
          MPIEXEC_FLAGS: --oversubscribe
        run: |
          cd build
          ctest -R "^Si_HSEX_Magnus$" --output-on-failure

      - name: Run GaAs_IPA test
        env:
          MPIEXEC_FLAGS: --oversubscribe
        run: |
          cd build
          ctest -R "^GaAs_IPA$" --output-on-failure

      - name: Run GaAs_HSEX test
        env:
          MPIEXEC_FLAGS: --oversubscribe
        run: |
          cd build
          ctest -R "^GaAs_HSEX$" --output-on-failure

      - name: Run LiF_IPA test
        env:
          MPIEXEC_FLAGS: --oversubscribe
        run: |
          cd build
          ctest -R "^LiF_IPA$" --output-on-failure

      - name: Run LiF_HSEX test
        env:
          MPIEXEC_FLAGS: --oversubscribe
        run: |
          cd build
          ctest -R "^LiF_HSEX$" --output-on-failure

      - name: Run MoS2_HSEX test
        env:
          MPIEXEC_FLAGS: --oversubscribe
        run: |
          cd build
          ctest -R "^MoS2_HSEX$" --output-on-failure

      - name: Run hBN_HSEX_np4 test
        env:
          MPIEXEC_FLAGS: --oversubscribe
        run: |
          cd build
          ctest -R "^hBN_HSEX_np4$" --output-on-failure

      - name: Run LiF_HSEX_np4 test
        env:
          MPIEXEC_FLAGS: --oversubscribe
        run: |
          cd build
          ctest -R "^LiF_HSEX_np4$" --output-on-failure
